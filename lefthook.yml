pre-commit:
  commands:
    ls-lint:
      run: pnpm exec ls-lint src

    ultracite:
      glob: "*.{js,jsx,ts,tsx,json,jsonc,css}"
      run: pnpm dlx ultracite fix {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: pnpm dlx commitlint --edit {1}
